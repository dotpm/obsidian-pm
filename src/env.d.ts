/** From tsdown `define`: true in dev builds, false in production unless STYLEGUIDE=1. */
declare const __STYLEGUIDE__: boolean
/** From tsdown `define`: the built viewer page, or empty when it was not built. */
declare const __VIEWER_TEMPLATE__: string
/** From tsdown `define`: the contents of CHANGELOG.md. */
declare const __CHANGELOG__: string
