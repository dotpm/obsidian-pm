{
  "name": "@dotpm/core",
  "version": "0.1.0",
  "description": "The dotpm domain: project and task types, dates, filtering, scheduling and the YAML pipeline",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dotpm/obsidian-pm.git",
    "directory": "packages/core"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "publishConfig": {
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    },
    "access": "public"
  },
  "scripts": {
    "build": "tsdown",
    "check:package": "publint --pack pnpm && pnpm pack --out pack.tgz && attw --profile esm-only pack.tgz && rm pack.tgz"
  },
  "dependencies": {
    "temporal-polyfill": "1.0.4"
  }
}
